[project]
name = "openclaw-memgpt-sidecar-server"
version = "0.1.0"
description = "FastAPI server for the OpenClaw MemGPT sidecar — wraps the published openclaw-memgpt-sidecar (memgpt) package"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "openclaw-memgpt-sidecar==1.0.0",
]
