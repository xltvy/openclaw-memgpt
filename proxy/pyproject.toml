[project]
name = "openclaw-memgpt-proxy"
version = "0.1.0"
description = "LiteLLM proxy + shim for OpenClaw MemGPT plugin (V1 testing)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "litellm[proxy]>=1.50",
    "openai>=1.0",
]
